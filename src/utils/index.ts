import * as Validations from "./validations";
import date from "./dateInfo";
import encodeImageFileAsURL from "./encodeImageFileAsURL";
import handleCutText from "./cutText"

const UTILS = { Validations, date, encodeImageFileAsURL , handleCutText };

export default UTILS;
